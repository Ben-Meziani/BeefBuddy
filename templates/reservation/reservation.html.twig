<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Confirmation - BeefBuddy</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            color: #333;
            line-height: 1.6;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background: white;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            border-radius: 8px;
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            font-weight: 300;
        }

        .header .subtitle {
            font-size: 1.2em;
            opacity: 0.9;
        }

        .bill-info {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            padding: 30px;
            border-bottom: 2px solid #ecf0f1;
        }

        .bill-section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 6px;
            border-left: 4px solid #3498db;
        }

        .bill-section h3 {
            color: #2c3e50;
            margin-bottom: 15px;
            font-size: 1.1em;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            padding: 5px 0;
        }

        .info-row .label {
            font-weight: 600;
            color: #555;
        }

        .info-row .value {
            color: #333;
        }

        .reservation-details {
            padding: 30px;
            border-bottom: 2px solid #ecf0f1;
        }

        .reservation-details h2 {
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 1.5em;
            border-bottom: 2px solid #3498db;
            padding-bottom: 10px;
        }

        .fighter-card {
            background: linear-gradient(135deg, #e74c3c, #c0392b);
            color: white;
            padding: 25px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .fighter-name {
            font-size: 1.8em;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .fighter-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .stat-item {
            text-align: center;
            background: rgba(255,255,255,0.1);
            padding: 10px;
            border-radius: 6px;
        }

        .stat-value {
            font-size: 1.5em;
            font-weight: bold;
            display: block;
        }

        .stat-label {
            font-size: 0.9em;
            opacity: 0.9;
        }

        .pricing-section {
            padding: 30px;
            background: #f8f9fa;
        }

        .pricing-section h2 {
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 1.5em;
        }

        .price-breakdown {
            background: white;
            padding: 20px;
            border-radius: 6px;
            border: 1px solid #dee2e6;
        }

        .price-row {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }

        .price-row:last-child {
            border-bottom: none;
            font-weight: bold;
            font-size: 1.2em;
            color: #2c3e50;
            border-top: 2px solid #3498db;
            padding-top: 15px;
            margin-top: 10px;
        }

        .payment-status {
            padding: 30px;
            text-align: center;
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
        }

        .payment-status h2 {
            font-size: 2em;
            margin-bottom: 15px;
        }

        .status-icon {
            font-size: 4em;
            margin-bottom: 20px;
        }

        .footer {
            background: #2c3e50;
            color: white;
            text-align: center;
            padding: 20px;
            font-size: 0.9em;
        }

        .footer a {
            color: #3498db;
            text-decoration: none;
        }

        .footer a:hover {
            text-decoration: underline;
        }

        @media print {
            body { background: white; }
            .container { box-shadow: none; margin: 0; }
        }

        @media (max-width: 768px) {
            .bill-info {
                grid-template-columns: 1fr;
                gap: 20px;
            }

            .fighter-stats {
                grid-template-columns: repeat(2, 1fr);
            }

            .container {
                margin: 10px;
                border-radius: 0;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>BeefBuddy</h1>
            <div class="subtitle">Payment Confirmation</div>
        </div>

        <!-- Bill Information -->
        <div class="bill-info">
            <div class="bill-section">
                <h3>Bill To</h3>
                <div class="info-row">
                    <span class="label">Name:</span>
                    <span class="value">{{ reservation.user.username|default('N/A') }}</span>
                </div>
                <div class="info-row">
                    <span class="label">Email:</span>
                    <span class="value">{{ reservation.user.email|default('N/A') }}</span>
                </div>
                <div class="info-row">
                    <span class="label">User ID:</span>
                    <span class="value">#{{ reservation.user.id|default('N/A') }}</span>
                </div>
            </div>

            <div class="bill-section">
                <h3>Reservation Details</h3>
                <div class="info-row">
                    <span class="label">Reservation ID:</span>
                    <span class="value">#{{ reservation.id|default('N/A') }}</span>
                </div>
                <div class="info-row">
                    <span class="label">Date:</span>
                    <span class="value">{{ reservation.startAt|date('F j, Y')|default('N/A') }}</span>
                </div>
                <div class="info-row">
                    <span class="label">Time:</span>
                    <span class="value">{{ reservation.startAt|date('g:i A')|default('N/A') }} - {{ reservation.endAt|date('g:i A')|default('N/A') }}</span>
                </div>
                <div class="info-row">
                    <span class="label">Status:</span>
                    <span class="value" style="color: #27ae60; font-weight: bold;">CONFIRMED</span>
                </div>
            </div>
        </div>

        <!-- Reservation Details -->
        <div class="reservation-details">
            <h2>Training Session Details</h2>

            <div class="fighter-card">
                                <div class="fighter-name">
                    {% if reservation.fighter.nickname %}
                        "{{ reservation.fighter.nickname }}"
                    {% endif %}
                    {{ reservation.fighter.name|default('N/A') }}
                </div>
                <div style="margin-bottom: 15px;">
                    <strong>{{ reservation.fighter.weightClass|default('N/A') }}</strong> • {{ reservation.fighter.country|default('N/A') }}
                </div>

                <div class="fighter-stats">
                    <div class="stat-item">
                        <span class="stat-value">{{ reservation.fighter.wins|default(0) }}</span>
                        <span class="stat-label">Wins</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">{{ reservation.fighter.losses|default(0) }}</span>
                        <span class="stat-label">Losses</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">{{ reservation.fighter.draws|default(0) }}</span>
                        <span class="stat-label">Draws</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-value">{{ reservation.fighter.pricePerTraining|default(0) }}</span>
                        <span class="stat-label">$/Session</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Pricing Section -->
        <div class="pricing-section">
            <h2>Payment Summary</h2>
            <div class="price-breakdown">
                <div class="price-row">
                    <span>Training Session ({{ reservation.startAt|date('M j')|default('N/A') }})</span>
                    <span>${{ reservation.fighter.pricePerTraining|default(0) }}</span>
                </div>
                <div class="price-row">
                    <span>Duration</span>
                    <span>{{ reservation.startAt|date('H:i')|default('N/A') }} - {{ reservation.endAt|date('H:i')|default('N/A') }}</span>
                </div>
                <div class="price-row">
                    <span>Total Amount</span>
                    <span>${{ reservation.totalPrice|default(0) }}</span>
                </div>
            </div>
        </div>

        <!-- Payment Status -->
        <div class="payment-status">
            <div class="status-icon">✓</div>
            <h2>Payment Confirmed!</h2>
            <p>Your training session has been successfully booked and payment has been processed.</p>
            <p style="margin-top: 15px; font-size: 1.1em;">
                <strong>Transaction ID:</strong> TXN-{{ reservation.id|default('N/A') }}-{{ "now"|date('Ymd') }}
            </p>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>Thank you for choosing BeefBuddy for your training needs!</p>
            <p>For any questions, please contact us at <a href="mailto:support@beefbuddy.com">support@beefbuddy.com</a></p>
            <p style="margin-top: 15px; opacity: 0.8;">
                This is an official payment confirmation. Please keep this for your records.
            </p>
        </div>
    </div>
</body>
</html>
